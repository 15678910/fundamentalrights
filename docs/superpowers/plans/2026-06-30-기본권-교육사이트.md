# 우리 헌법 기본권 교육 사이트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 대한민국 헌법 제2장 기본권을 쉬운 말/보통/자세히 3단계로 전환해 읽는 Astro 정적 교육 사이트를 만든다.

**Architecture:** Astro Content Collections로 "권리 1개 = Markdown 1파일"을 관리하고, 빌드 시 정적 페이지를 생성한다. 난이도 토글·퀴즈·접근성은 최소한의 vanilla JS로 처리하고, 검색은 Pagefind 정적 인덱스를 쓴다. 순수 정적 산출물이라 GitHub Pages에 무료 배포한다.

**Tech Stack:** Astro, TypeScript, Astro Content Collections(zod 스키마), Vitest(단위 테스트), Pagefind(정적 검색), Pretendard 웹폰트, GitHub Pages.

> **환경 주의:** Windows + PowerShell. 모든 명령은 프로젝트 루트 `C:\Users\lacoi\Desktop\fundamentalrights`에서 실행. git이 아직 초기화되지 않았으므로 Task 0에서 초기화한다.

---

## 파일 구조

```
fundamentalrights/
  astro.config.mjs            # Astro + Pagefind 통합 설정, site/base
  package.json
  tsconfig.json
  vitest.config.ts
  src/
    content/
      config.ts               # rights / glossary 컬렉션 zod 스키마
      rights/                  # 권리별 Markdown (10-인간의-존엄.md ...)
      glossary/                # 용어별 Markdown 또는 단일 데이터
    lib/
      categories.ts           # 6+1 범주 정의(키·라벨·색·순서)
      quiz.ts                  # 퀴즈 채점 순수 함수(테스트 대상)
      difficulty.ts           # 난이도 키·라벨·기본값(테스트 대상)
    components/
      DifficultyToggle.astro   # 쉬운말/보통/자세히 전환 UI + JS
      OriginalText.astro       # 헌법 원문 펼침(accordion)
      Quiz.astro               # 퀴즈 카드 + 채점 JS
      AccessibilityBar.astro   # 고대비/글자크기 토글
      RightCard.astro          # 홈의 권리 카드
      Mascot.astro             # 범주별 마스코트(자리표시)
    layouts/
      BaseLayout.astro         # head, 헤더/푸터, 전역 CSS, 접근성, 검색창
    pages/
      index.astro             # 홈(범주별 카드 그리드)
      about.astro             # 소개·출처·면책
      glossary.astro          # 용어 사전
      rights/[slug].astro     # 권리 상세
    styles/
      global.css              # 디자인 토큰(색/타이포), 접근성, print
    scripts/
      difficulty-toggle.ts    # 클라이언트: 토글 + localStorage
      quiz-client.ts          # 클라이언트: 퀴즈 채점 표시
      a11y.ts                 # 클라이언트: 고대비/글자크기 + localStorage
  tests/
    quiz.test.ts
    difficulty.test.ts
    content-schema.test.ts
```

---

## Task 0: 프로젝트 초기화 및 git

**Files:**
- Create: `package.json`, `astro.config.mjs`, `tsconfig.json`, `.gitignore`

- [ ] **Step 1: git 초기화**

Run:
```bash
git init
```

- [ ] **Step 2: Astro + 의존성 설치**

Run (PowerShell, 루트에서):
```bash
npm create astro@latest -- --template minimal --no-install --no-git --yes .
npm install
npm install -D vitest @astrojs/check typescript
npm install pagefind
```
Expected: `package.json`, `src/`, `astro.config.mjs` 생성. 설치 오류 없음.

- [ ] **Step 3: .gitignore 보강**

`.gitignore`에 아래 줄이 포함되어야 함(없으면 추가):
```
node_modules/
dist/
.astro/
.superpowers/
```

- [ ] **Step 4: package.json 스크립트 확인/추가**

`package.json`의 `"scripts"`에 아래가 있어야 함:
```json
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run"
  }
}
```

- [ ] **Step 5: 빌드 확인**

Run:
```bash
npm run build
```
Expected: 기본 템플릿 빌드 성공, `dist/` 생성.

- [ ] **Step 6: 커밋**

```bash
git add -A
git commit -m "chore: scaffold Astro project"
```

---

## Task 1: 난이도 정의 + 단위 테스트

**Files:**
- Create: `src/lib/difficulty.ts`
- Create: `vitest.config.ts`
- Test: `tests/difficulty.test.ts`

- [ ] **Step 1: vitest 설정 작성**

`vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    include: ["tests/**/*.test.ts"],
    environment: "node",
  },
});
```

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/difficulty.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { DIFFICULTIES, DEFAULT_DIFFICULTY, isDifficulty } from "../src/lib/difficulty";

describe("difficulty", () => {
  it("정확히 3단계를 순서대로 정의한다", () => {
    expect(DIFFICULTIES.map((d) => d.key)).toEqual(["easy", "normal", "detailed"]);
  });

  it("기본값은 easy(쉬운 말)이다", () => {
    expect(DEFAULT_DIFFICULTY).toBe("easy");
  });

  it("유효한 난이도 키를 판별한다", () => {
    expect(isDifficulty("normal")).toBe(true);
    expect(isDifficulty("expert")).toBe(false);
  });
});
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — `src/lib/difficulty` 모듈 없음.

- [ ] **Step 4: 구현**

`src/lib/difficulty.ts`:
```ts
export type DifficultyKey = "easy" | "normal" | "detailed";

export interface Difficulty {
  key: DifficultyKey;
  label: string;   // 화면 표시
  heading: string; // Markdown 안의 ## 제목과 매칭
}

export const DIFFICULTIES: Difficulty[] = [
  { key: "easy", label: "쉬운 말", heading: "쉬운 말" },
  { key: "normal", label: "보통", heading: "보통" },
  { key: "detailed", label: "자세히", heading: "자세히" },
];

export const DEFAULT_DIFFICULTY: DifficultyKey = "easy";

export function isDifficulty(value: string): value is DifficultyKey {
  return DIFFICULTIES.some((d) => d.key === value);
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `npm test`
Expected: PASS (3 tests).

- [ ] **Step 6: 커밋**

```bash
git add src/lib/difficulty.ts vitest.config.ts tests/difficulty.test.ts
git commit -m "feat: difficulty levels with tests"
```

---

## Task 2: 퀴즈 채점 순수 함수 + 테스트

**Files:**
- Create: `src/lib/quiz.ts`
- Test: `tests/quiz.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/quiz.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { gradeQuiz, type QuizQuestion } from "../src/lib/quiz";

const q: QuizQuestion = {
  question: "인간의 존엄은 누구에게 있을까요?",
  options: ["모든 사람", "어른만"],
  answer: 0,
  explanation: "모든 사람에게 똑같이 있어요.",
};

describe("gradeQuiz", () => {
  it("정답 선택 시 correct=true", () => {
    expect(gradeQuiz(q, 0)).toEqual({ correct: true, explanation: q.explanation });
  });
  it("오답 선택 시 correct=false", () => {
    expect(gradeQuiz(q, 1)).toEqual({ correct: false, explanation: q.explanation });
  });
  it("범위 밖 선택은 오답 처리", () => {
    expect(gradeQuiz(q, 5).correct).toBe(false);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — `src/lib/quiz` 없음.

- [ ] **Step 3: 구현**

`src/lib/quiz.ts`:
```ts
export interface QuizQuestion {
  question: string;
  options: string[];
  answer: number;       // 정답 인덱스
  explanation: string;
}

export interface QuizResult {
  correct: boolean;
  explanation: string;
}

export function gradeQuiz(q: QuizQuestion, selected: number): QuizResult {
  return { correct: selected === q.answer, explanation: q.explanation };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/lib/quiz.ts tests/quiz.test.ts
git commit -m "feat: quiz grading function with tests"
```

---

## Task 3: 범주 정의

**Files:**
- Create: `src/lib/categories.ts`

- [ ] **Step 1: 구현**

`src/lib/categories.ts`:
```ts
export interface Category {
  key: string;
  label: string;
  color: string; // 카드/태그 색
  order: number;
}

export const CATEGORIES: Category[] = [
  { key: "comprehensive", label: "포괄적 기본권", color: "#7c3aed", order: 1 },
  { key: "equality",      label: "평등권",        color: "#2563eb", order: 2 },
  { key: "freedom",       label: "자유권",        color: "#0891b2", order: 3 },
  { key: "participation", label: "참정권",        color: "#16a34a", order: 4 },
  { key: "claim",         label: "청구권",        color: "#ca8a04", order: 5 },
  { key: "social",        label: "사회권",        color: "#db2777", order: 6 },
  { key: "duty",          label: "국민의 의무",    color: "#64748b", order: 7 },
];

export const CATEGORY_KEYS = CATEGORIES.map((c) => c.key);

export function getCategory(key: string): Category | undefined {
  return CATEGORIES.find((c) => c.key === key);
}
```

- [ ] **Step 2: 커밋**

```bash
git add src/lib/categories.ts
git commit -m "feat: rights categories"
```

---

## Task 4: Content Collection 스키마 + 스키마 테스트

**Files:**
- Create: `src/content/config.ts`
- Create: `src/content/rights/10-인간의-존엄.md` (첫 샘플)
- Test: `tests/content-schema.test.ts`

- [ ] **Step 1: 스키마 작성**

`src/content/config.ts` (Astro 5 content layer / glob loader API):
```ts
import { defineCollection, z } from "astro:content";
import { glob } from "astro/loaders";
import { CATEGORY_KEYS } from "../lib/categories";

const quizQuestion = z.object({
  question: z.string(),
  options: z.array(z.string()).min(2),
  answer: z.number().int().min(0),
  explanation: z.string(),
});

const rights = defineCollection({
  loader: glob({ pattern: "**/*.md", base: "./src/content/rights" }),
  schema: z.object({
    article: z.number().int(),               // 조문번호
    title: z.string(),                       // 권리 제목
    category: z.enum(CATEGORY_KEYS as [string, ...string[]]),
    icon: z.string().default("default"),
    original: z.string(),                    // 헌법 원문
    terms: z.array(z.string()).default([]),  // 관련 용어 키
    quiz: z.array(quizQuestion).default([]),
    order: z.number().int(),                 // 표시 순서
  }),
});

const glossary = defineCollection({
  loader: glob({ pattern: "**/*.md", base: "./src/content/glossary" }),
  schema: z.object({
    term: z.string(),
    short: z.string(),  // 한 줄 쉬운 설명(툴팁)
  }),
});

export const collections = { rights, glossary };
```

> **Astro 5 주의:** glob 로더를 쓰면 각 엔트리의 식별자는 `entry.id`(확장자 없는
> 파일명, 예: `10-인간의-존엄`)이며, `entry.slug`는 없다. 본문 렌더링은
> `import { render } from "astro:content"; const { Content } = await render(entry);`
> 형태를 쓴다. (레거시 `entry.render()`/`entry.slug`는 사용하지 않는다.)

- [ ] **Step 2: 본문 작성 규칙 메모**

권리 Markdown 본문은 반드시 `## 쉬운 말`, `## 보통`, `## 자세히` 세 섹션을 이 순서로 포함한다(난이도 토글이 이 제목으로 영역을 구분).

- [ ] **Step 3: 첫 샘플 권리 작성**

`src/content/rights/10-인간의-존엄.md`:
```markdown
---
article: 10
title: 인간의 존엄과 행복추구권
category: comprehensive
icon: dignity
order: 1
original: "모든 국민은 인간으로서의 존엄과 가치를 가지며, 행복을 추구할 권리를 가진다. 국가는 개인이 가지는 불가침의 기본적 인권을 확인하고 이를 보장할 의무를 진다."
terms: [기본권, 행복추구권]
quiz:
  - question: 인간의 존엄은 누구에게 있을까요?
    options: [모든 사람, 어른만]
    answer: 0
    explanation: 인간의 존엄은 나이와 상관없이 모든 사람에게 똑같이 있어요.
---

## 쉬운 말

모든 사람은 그 자체로 소중해요. 키가 크든 작든, 어디서 왔든 누구도 나를 함부로 대하거나 무시할 수 없어요. 그리고 누구나 자기가 행복해지는 길을 스스로 고를 수 있어요.

## 보통

모든 국민은 인간으로서 존엄과 가치를 가집니다. 이는 사람을 수단이 아니라 그 자체로 존중해야 한다는 뜻입니다. 또한 누구나 자신의 삶을 스스로 결정하고 행복을 추구할 권리가 있으며, 국가는 이러한 기본적 인권을 보장할 의무가 있습니다.

## 자세히

헌법 제10조는 인간의 존엄과 가치, 행복추구권을 규정하며 모든 기본권의 이념적 출발점으로 평가됩니다. 헌법재판소는 이 조항으로부터 일반적 행동자유권과 인격권 등을 도출해 왔습니다. 국가는 개인의 불가침의 기본적 인권을 '확인'하고 '보장'할 의무를 지므로, 이는 단순한 선언이 아니라 적극적 보호의무의 근거가 됩니다.
```

- [ ] **Step 4: 스키마 검증 테스트 작성**

`tests/content-schema.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";

// 프런트매터의 필수 섹션 존재를 가볍게 검증(astro check가 zod는 검증함)
describe("샘플 권리 본문", () => {
  const md = readFileSync("src/content/rights/10-인간의-존엄.md", "utf-8");
  it("세 난이도 섹션을 모두 포함한다", () => {
    expect(md).toContain("## 쉬운 말");
    expect(md).toContain("## 보통");
    expect(md).toContain("## 자세히");
  });
});
```

- [ ] **Step 5: 테스트 + 타입체크 통과 확인**

Run:
```bash
npm test
npm run check
```
Expected: vitest PASS, `astro check`가 content 스키마 오류 없이 통과.

- [ ] **Step 6: 커밋**

```bash
git add src/content/config.ts src/content/rights/10-인간의-존엄.md tests/content-schema.test.ts
git commit -m "feat: content schema and first right"
```

---

## Task 5: 디자인 토큰 + BaseLayout

**Files:**
- Create: `src/styles/global.css`
- Create: `src/layouts/BaseLayout.astro`

- [ ] **Step 1: 전역 CSS 작성**

`src/styles/global.css`:
```css
:root {
  --bg: #ffffff;
  --fg: #1e293b;
  --muted: #64748b;
  --accent: #2563eb;
  --border: #e5e7eb;
  --card: #fafafa;
  --font-scale: 1;
  --maxw: 720px;
}
:root[data-contrast="high"] {
  --bg: #000000;
  --fg: #ffffff;
  --muted: #e2e8f0;
  --accent: #ffd400;
  --border: #ffffff;
  --card: #111111;
}
* { box-sizing: border-box; }
html { font-size: calc(100% * var(--font-scale)); }
body {
  margin: 0;
  background: var(--bg);
  color: var(--fg);
  font-family: "Pretendard", system-ui, -apple-system, sans-serif;
  line-height: 1.7;
}
main { max-width: var(--maxw); margin: 0 auto; padding: 1rem; }
a { color: var(--accent); }
.skip-link {
  position: absolute; left: -999px;
}
.skip-link:focus { left: 1rem; top: 1rem; background: var(--accent); color: #fff; padding: .5rem; }
:focus-visible { outline: 3px solid var(--accent); outline-offset: 2px; }

@media print {
  header, footer, .no-print { display: none !important; }
  [data-difficulty-section] { display: block !important; }
}
```

- [ ] **Step 2: BaseLayout 작성**

`src/layouts/BaseLayout.astro`:
```astro
---
import "../styles/global.css";
import AccessibilityBar from "../components/AccessibilityBar.astro";
const { title = "우리 헌법 기본권" } = Astro.props;
const base = import.meta.env.BASE_URL;
---
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="preconnect" href="https://cdn.jsdelivr.net" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css"
    />
  </head>
  <body>
    <a href="#main" class="skip-link">본문 바로가기</a>
    <header class="no-print" style="border-bottom:1px solid var(--border)">
      <nav style="max-width:var(--maxw);margin:0 auto;padding:.75rem 1rem;display:flex;gap:1rem;align-items:center">
        <a href={base} style="font-weight:700;text-decoration:none;color:var(--fg)">우리 헌법 기본권</a>
        <span style="flex:1"></span>
        <a href={`${base}glossary`}>용어사전</a>
        <a href={`${base}about`}>소개</a>
        <a href={`${base}search`}>검색</a>
        <AccessibilityBar />
      </nav>
    </header>
    <main id="main">
      <slot />
    </main>
    <footer class="no-print" style="border-top:1px solid var(--border);color:var(--muted);font-size:.85rem">
      <div style="max-width:var(--maxw);margin:0 auto;padding:1rem">
        본 설명은 학습용 요약이며 법적 효력이 있는 해석이 아닙니다. 정확한 내용은 헌법 원문과 공식 자료를 확인하세요.
      </div>
    </footer>
    <script src={`${base}scripts/a11y.js`} is:inline></script>
  </body>
</html>
```

> 참고: `a11y` 스크립트는 Task 8에서 작성. 이 단계에선 링크만 둔다.

- [ ] **Step 3: 빌드 확인(컴포넌트 누락 전 임시)**

이 단계에서는 `AccessibilityBar`가 아직 없으므로 Task 8 완료 후 함께 빌드한다. 여기서는 파일만 커밋.

- [ ] **Step 4: 커밋**

```bash
git add src/styles/global.css src/layouts/BaseLayout.astro
git commit -m "feat: design tokens and base layout"
```

---

## Task 6: 난이도 토글 + 원문 펼침 컴포넌트

**Files:**
- Create: `src/scripts/difficulty-toggle.ts`
- Create: `src/components/DifficultyToggle.astro`
- Create: `src/components/OriginalText.astro`

- [ ] **Step 1: 클라이언트 토글 스크립트**

`src/scripts/difficulty-toggle.ts`:
```ts
const KEY = "difficulty";
const VALID = ["easy", "normal", "detailed"];

function apply(level: string) {
  document.querySelectorAll<HTMLElement>("[data-difficulty-section]").forEach((el) => {
    el.hidden = el.dataset.difficultySection !== level;
  });
  document.querySelectorAll<HTMLButtonElement>("[data-difficulty-btn]").forEach((b) => {
    b.setAttribute("aria-pressed", String(b.dataset.difficultyBtn === level));
  });
}

function init() {
  const saved = localStorage.getItem(KEY);
  const level = saved && VALID.includes(saved) ? saved : "easy";
  apply(level);
  document.querySelectorAll<HTMLButtonElement>("[data-difficulty-btn]").forEach((b) => {
    b.addEventListener("click", () => {
      const lvl = b.dataset.difficultyBtn!;
      localStorage.setItem(KEY, lvl);
      apply(lvl);
    });
  });
}
init();
```

- [ ] **Step 2: 토글 컴포넌트**

`src/components/DifficultyToggle.astro`:
```astro
---
import { DIFFICULTIES } from "../lib/difficulty";
---
<div role="group" aria-label="난이도 선택" style="display:flex;gap:.5rem;flex-wrap:wrap;margin:1rem 0">
  {DIFFICULTIES.map((d) => (
    <button
      data-difficulty-btn={d.key}
      aria-pressed={d.key === "easy"}
      style="padding:.4rem .8rem;border:1px solid var(--border);border-radius:999px;background:var(--bg);color:var(--fg);cursor:pointer"
    >{d.label}</button>
  ))}
</div>
<style>
  button[aria-pressed="true"] { background: var(--fg); color: var(--bg); }
</style>
```

- [ ] **Step 3: 원문 펼침 컴포넌트**

`src/components/OriginalText.astro`:
```astro
---
const { text, article } = Astro.props;
---
<details style="margin:1rem 0;border:1px solid var(--border);border-radius:8px;padding:.5rem .75rem;background:var(--card)">
  <summary style="cursor:pointer;font-weight:600">📜 헌법 원문 (제{article}조)</summary>
  <p style="margin:.5rem 0 0;color:var(--muted)">{text}</p>
</details>
```

- [ ] **Step 4: 커밋**

```bash
git add src/scripts/difficulty-toggle.ts src/components/DifficultyToggle.astro src/components/OriginalText.astro
git commit -m "feat: difficulty toggle and original text components"
```

---

## Task 7: 퀴즈 컴포넌트

**Files:**
- Create: `src/scripts/quiz-client.ts`
- Create: `src/components/Quiz.astro`

- [ ] **Step 1: 퀴즈 클라이언트 스크립트**

`src/scripts/quiz-client.ts`:
```ts
document.querySelectorAll<HTMLElement>("[data-quiz]").forEach((quiz) => {
  const answer = Number(quiz.dataset.answer);
  const result = quiz.querySelector<HTMLElement>("[data-quiz-result]")!;
  quiz.querySelectorAll<HTMLButtonElement>("[data-quiz-option]").forEach((btn) => {
    btn.addEventListener("click", () => {
      const selected = Number(btn.dataset.quizOption);
      const correct = selected === answer;
      result.hidden = false;
      result.textContent = (correct ? "⭕ 정답! " : "❌ 아쉬워요. ") + (quiz.dataset.explanation ?? "");
      result.style.color = correct ? "#16a34a" : "#dc2626";
    });
  });
});
```

- [ ] **Step 2: 퀴즈 컴포넌트**

`src/components/Quiz.astro`:
```astro
---
const { questions = [] } = Astro.props;
---
{questions.map((q: any) => (
  <div
    data-quiz
    data-answer={q.answer}
    data-explanation={q.explanation}
    style="border-left:4px solid var(--accent);background:var(--card);border-radius:6px;padding:.75rem 1rem;margin:1rem 0"
  >
    <p style="font-weight:600;margin:.2rem 0">❓ {q.question}</p>
    <div style="display:flex;gap:.5rem;flex-wrap:wrap">
      {q.options.map((opt: string, i: number) => (
        <button
          data-quiz-option={i}
          style="padding:.4rem .8rem;border:1px solid var(--border);border-radius:8px;background:var(--bg);color:var(--fg);cursor:pointer"
        >{opt}</button>
      ))}
    </div>
    <p data-quiz-result hidden style="margin:.5rem 0 0;font-weight:600"></p>
  </div>
))}
```

- [ ] **Step 3: 커밋**

```bash
git add src/scripts/quiz-client.ts src/components/Quiz.astro
git commit -m "feat: quiz component"
```

---

## Task 8: 접근성 바 (고대비/글자 크기)

**Files:**
- Create: `src/scripts/a11y.ts`
- Create: `src/components/AccessibilityBar.astro`

- [ ] **Step 1: 접근성 클라이언트 스크립트**

`src/scripts/a11y.ts`:
```ts
const root = document.documentElement;

(function restore() {
  if (localStorage.getItem("contrast") === "high") root.dataset.contrast = "high";
  const scale = localStorage.getItem("fontScale");
  if (scale) root.style.setProperty("--font-scale", scale);
})();

document.addEventListener("click", (e) => {
  const t = (e.target as HTMLElement).closest<HTMLElement>("[data-a11y]");
  if (!t) return;
  const action = t.dataset.a11y;
  if (action === "contrast") {
    const on = root.dataset.contrast === "high";
    if (on) { delete root.dataset.contrast; localStorage.removeItem("contrast"); }
    else { root.dataset.contrast = "high"; localStorage.setItem("contrast", "high"); }
  }
  if (action === "font-up" || action === "font-down") {
    const cur = parseFloat(getComputedStyle(root).getPropertyValue("--font-scale")) || 1;
    const next = Math.min(1.5, Math.max(0.9, cur + (action === "font-up" ? 0.1 : -0.1)));
    root.style.setProperty("--font-scale", String(next));
    localStorage.setItem("fontScale", String(next));
  }
});
```

- [ ] **Step 2: a11y 스크립트를 public으로 노출**

BaseLayout이 `${base}scripts/a11y.js`를 참조하므로, 빌드 시 포함되도록 `public/scripts/`에 컴파일 결과를 두는 대신 간단히 동일 내용을 `public/scripts/a11y.js`(plain JS)로 복사한다.

`public/scripts/a11y.js` (위 Step 1 코드에서 타입 표기만 제거한 동일 로직):
```js
const root = document.documentElement;
(function restore(){
  if (localStorage.getItem("contrast") === "high") root.dataset.contrast = "high";
  const scale = localStorage.getItem("fontScale");
  if (scale) root.style.setProperty("--font-scale", scale);
})();
document.addEventListener("click", (e) => {
  const t = e.target.closest("[data-a11y]");
  if (!t) return;
  const action = t.dataset.a11y;
  if (action === "contrast") {
    const on = root.dataset.contrast === "high";
    if (on) { delete root.dataset.contrast; localStorage.removeItem("contrast"); }
    else { root.dataset.contrast = "high"; localStorage.setItem("contrast", "high"); }
  }
  if (action === "font-up" || action === "font-down") {
    const cur = parseFloat(getComputedStyle(root).getPropertyValue("--font-scale")) || 1;
    const next = Math.min(1.5, Math.max(0.9, cur + (action === "font-up" ? 0.1 : -0.1)));
    root.style.setProperty("--font-scale", String(next));
    localStorage.setItem("fontScale", String(next));
  }
});
```

- [ ] **Step 3: 접근성 바 컴포넌트**

`src/components/AccessibilityBar.astro`:
```astro
<div style="display:flex;gap:.3rem">
  <button data-a11y="contrast" aria-label="고대비 켜고 끄기" style="border:1px solid var(--border);border-radius:6px;background:var(--bg);color:var(--fg);cursor:pointer;padding:.2rem .5rem">◐</button>
  <button data-a11y="font-up" aria-label="글자 크게" style="border:1px solid var(--border);border-radius:6px;background:var(--bg);color:var(--fg);cursor:pointer;padding:.2rem .5rem">A+</button>
  <button data-a11y="font-down" aria-label="글자 작게" style="border:1px solid var(--border);border-radius:6px;background:var(--bg);color:var(--fg);cursor:pointer;padding:.2rem .5rem">A−</button>
</div>
```

- [ ] **Step 4: 커밋**

```bash
git add src/scripts/a11y.ts public/scripts/a11y.js src/components/AccessibilityBar.astro
git commit -m "feat: accessibility bar (contrast + font size)"
```

---

## Task 9: 권리 상세 페이지

**Files:**
- Create: `src/pages/rights/[slug].astro`

- [ ] **Step 1: 동적 라우트 작성**

`src/pages/rights/[slug].astro`:
```astro
---
import { getCollection, render } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import DifficultyToggle from "../../components/DifficultyToggle.astro";
import OriginalText from "../../components/OriginalText.astro";
import Quiz from "../../components/Quiz.astro";
import { getCategory } from "../../lib/categories";
import { DIFFICULTIES } from "../../lib/difficulty";

export async function getStaticPaths() {
  const rights = await getCollection("rights");
  return rights.map((r) => ({ params: { slug: r.id }, props: { entry: r } }));
}

const { entry } = Astro.props;
const { Content, headings } = await render(entry);
const cat = getCategory(entry.data.category);
const base = import.meta.env.BASE_URL;
---
<BaseLayout title={`${entry.data.title} | 우리 헌법 기본권`}>
  <p style="color:var(--muted);font-size:.85rem">
    <a href={base}>홈</a> ›
    <span style={`color:${cat?.color}`}>{cat?.label}</span>
  </p>
  <h1 style="margin:.2rem 0">제{entry.data.article}조 · {entry.data.title}</h1>

  <DifficultyToggle />

  <article>
    <Content />
  </article>

  <OriginalText text={entry.data.original} article={entry.data.article} />

  {entry.data.quiz.length > 0 && <Quiz questions={entry.data.quiz} />}

  <script src={`${base}scripts/difficulty-toggle.js`} is:inline></script>
  <script src={`${base}scripts/quiz-client.js`} is:inline></script>
</BaseLayout>
```

- [ ] **Step 2: 난이도 섹션 래핑 처리**

Astro의 `<Content />`는 Markdown을 그대로 렌더하므로, 난이도 토글이 `[data-difficulty-section]`을 찾을 수 있도록 렌더 후 클라이언트에서 `## 쉬운 말/보통/자세히` 헤딩을 기준으로 감싸야 한다. 이를 위해 `difficulty-toggle.js` 상단에 섹션 래핑 로직을 추가한다.

`public/scripts/difficulty-toggle.js` (Task 6 TS의 plain JS 버전 + 래핑):
```js
(function wrapSections(){
  const map = { "쉬운 말":"easy", "보통":"normal", "자세히":"detailed" };
  const article = document.querySelector("article");
  if (!article) return;
  const nodes = Array.from(article.childNodes);
  let current = null;
  nodes.forEach((node) => {
    if (node.nodeType === 1 && node.tagName === "H2" && map[node.textContent.trim()]) {
      current = document.createElement("section");
      current.setAttribute("data-difficulty-section", map[node.textContent.trim()]);
      node.replaceWith(current);
      current.appendChild(node);
    } else if (current) {
      current.appendChild(node);
    }
  });
})();
const KEY = "difficulty";
const VALID = ["easy","normal","detailed"];
function apply(level){
  document.querySelectorAll("[data-difficulty-section]").forEach((el)=>{ el.hidden = el.getAttribute("data-difficulty-section") !== level; });
  document.querySelectorAll("[data-difficulty-btn]").forEach((b)=>{ b.setAttribute("aria-pressed", String(b.getAttribute("data-difficulty-btn") === level)); });
}
(function init(){
  const saved = localStorage.getItem(KEY);
  const level = saved && VALID.includes(saved) ? saved : "easy";
  apply(level);
  document.querySelectorAll("[data-difficulty-btn]").forEach((b)=>{
    b.addEventListener("click", ()=>{ const lvl=b.getAttribute("data-difficulty-btn"); localStorage.setItem(KEY,lvl); apply(lvl); });
  });
})();
```

또한 `public/scripts/quiz-client.js`를 Task 7 TS의 plain JS 버전으로 생성한다:
```js
document.querySelectorAll("[data-quiz]").forEach((quiz)=>{
  const answer = Number(quiz.dataset.answer);
  const result = quiz.querySelector("[data-quiz-result]");
  quiz.querySelectorAll("[data-quiz-option]").forEach((btn)=>{
    btn.addEventListener("click", ()=>{
      const selected = Number(btn.dataset.quizOption);
      const correct = selected === answer;
      result.hidden = false;
      result.textContent = (correct ? "⭕ 정답! " : "❌ 아쉬워요. ") + (quiz.dataset.explanation || "");
      result.style.color = correct ? "#16a34a" : "#dc2626";
    });
  });
});
```

- [ ] **Step 3: 빌드 + 미리보기 확인**

Run:
```bash
npm run build
npm run preview
```
Expected: 빌드 성공. 브라우저에서 `/rights/10-인간의-존엄` 접속 시 난이도 버튼이 보이고, 클릭하면 본문이 쉬운 말/보통/자세히로 전환되며, 원문 펼침과 퀴즈가 동작.

- [ ] **Step 4: 커밋**

```bash
git add src/pages/rights/[slug].astro public/scripts/difficulty-toggle.js public/scripts/quiz-client.js
git commit -m "feat: right detail page with working toggle and quiz"
```

---

## Task 10: 홈 페이지 (범주별 카드 그리드)

**Files:**
- Create: `src/components/RightCard.astro`
- Create: `src/components/Mascot.astro`
- Create: `src/pages/index.astro`

- [ ] **Step 1: 마스코트(자리표시) 컴포넌트**

`src/components/Mascot.astro`:
```astro
---
const { color = "#2563eb", emoji = "🧒" } = Astro.props;
---
<div style={`width:48px;height:48px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:24px;background:${color}22`}>{emoji}</div>
```

- [ ] **Step 2: 권리 카드 컴포넌트**

`src/components/RightCard.astro`:
```astro
---
import Mascot from "./Mascot.astro";
import { getCategory } from "../lib/categories";
const { entry } = Astro.props;
const cat = getCategory(entry.data.category);
const base = import.meta.env.BASE_URL;
---
<a href={`${base}rights/${entry.id}`} style="display:flex;gap:.75rem;align-items:center;border:1px solid var(--border);border-radius:10px;padding:.75rem;text-decoration:none;color:var(--fg)">
  <Mascot color={cat?.color} />
  <span>
    <span style={`font-size:.75rem;color:${cat?.color}`}>제{entry.data.article}조 · {cat?.label}</span>
    <strong style="display:block">{entry.data.title}</strong>
  </span>
</a>
```

- [ ] **Step 3: 홈 페이지**

`src/pages/index.astro`:
```astro
---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import RightCard from "../components/RightCard.astro";
import { CATEGORIES } from "../lib/categories";

const rights = (await getCollection("rights")).sort((a, b) => a.data.order - b.data.order);
---
<BaseLayout>
  <h1>우리 헌법이 지켜주는 권리</h1>
  <p style="color:var(--muted)">대한민국 헌법 제2장의 기본권을 <b>쉬운 말 · 보통 · 자세히</b> 세 단계로 읽어보세요. 누구나 자기 수준에서 이해할 수 있어요.</p>

  {CATEGORIES.map((cat) => {
    const items = rights.filter((r) => r.data.category === cat.key);
    if (items.length === 0) return null;
    return (
      <section style="margin:1.5rem 0">
        <h2 style={`color:${cat.color};border-bottom:2px solid ${cat.color};padding-bottom:.2rem`}>{cat.label}</h2>
        <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(240px,1fr));gap:.75rem;margin-top:.75rem">
          {items.map((entry) => <RightCard entry={entry} />)}
        </div>
      </section>
    );
  })}
</BaseLayout>
```

- [ ] **Step 4: 빌드 확인**

Run: `npm run build`
Expected: 성공. 미리보기에서 홈에 범주별 권리 카드가 표시됨.

- [ ] **Step 5: 커밋**

```bash
git add src/components/RightCard.astro src/components/Mascot.astro src/pages/index.astro
git commit -m "feat: home page with category grid"
```

---

## Task 11: 용어 사전 + about

**Files:**
- Create: `src/content/glossary/기본권.md`, `src/content/glossary/행복추구권.md`
- Create: `src/pages/glossary.astro`
- Create: `src/pages/about.astro`

- [ ] **Step 1: 용어 샘플 작성**

`src/content/glossary/기본권.md`:
```markdown
---
term: 기본권
short: 헌법이 모든 사람에게 보장하는 기본적인 권리예요.
---
기본권은 사람이라면 누구나 마땅히 누려야 할 권리로, 헌법이 직접 보장합니다.
```

`src/content/glossary/행복추구권.md`:
```markdown
---
term: 행복추구권
short: 내가 행복해지는 길을 스스로 고를 수 있는 권리예요.
---
행복추구권은 자신의 삶을 스스로 결정하고 행복을 추구할 수 있는 권리입니다.
```

- [ ] **Step 2: 용어 사전 페이지**

`src/pages/glossary.astro`:
```astro
---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
const terms = (await getCollection("glossary")).sort((a, b) => a.data.term.localeCompare(b.data.term, "ko"));
---
<BaseLayout title="용어 사전 | 우리 헌법 기본권">
  <h1>용어 사전</h1>
  <dl>
    {terms.map((t) => (
      <div id={t.data.term} style="margin:1rem 0;padding-bottom:1rem;border-bottom:1px solid var(--border)">
        <dt style="font-weight:700">{t.data.term}</dt>
        <dd style="margin:.3rem 0 0">{t.data.short}</dd>
      </div>
    ))}
  </dl>
</BaseLayout>
```

- [ ] **Step 3: about 페이지(출처·면책)**

`src/pages/about.astro`:
```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout title="소개 | 우리 헌법 기본권">
  <h1>이 사이트에 대하여</h1>
  <p>대한민국 헌법 제2장의 기본권을 초등학생부터 성인까지 누구나 이해할 수 있도록 쉬운 말·보통·자세히 세 단계로 설명합니다. 독일의 Grundrechte-Fibel과 bpb의 쉬운 정치 교육 자료에서 영감을 받았습니다.</p>
  <h2>출처</h2>
  <ul>
    <li>대한민국헌법 (국가법령정보센터)</li>
    <li>헌법재판소 주요 판례</li>
    <li>국가인권위원회 인권교육 자료</li>
  </ul>
  <h2>면책</h2>
  <p style="color:var(--muted)">본 설명은 학습용 요약이며 법적 효력이 있는 해석이 아닙니다. 정확한 내용은 헌법 원문과 공식 자료를 확인하세요. 배포 전 전문가·교사의 감수를 권장합니다.</p>
</BaseLayout>
```

- [ ] **Step 4: 본문 용어 링크(선택적 강화)**

권리 본문에서 용어를 `[기본권](../../glossary#기본권)` 형태의 Markdown 링크로 작성하면 자동으로 용어 사전 항목으로 연결된다(콘텐츠 작성 규칙으로 채택).

- [ ] **Step 5: 빌드 확인 + 커밋**

Run: `npm run build`
Expected: 성공.
```bash
git add src/content/glossary src/pages/glossary.astro src/pages/about.astro
git commit -m "feat: glossary and about pages"
```

---

## Task 12: Pagefind 검색

**Files:**
- Modify: `package.json` (build 스크립트)
- Create: `src/pages/search.astro`

- [ ] **Step 1: 빌드 후 Pagefind 인덱싱 추가**

`package.json`의 build 스크립트를 수정:
```json
"build": "astro build && pagefind --site dist"
```

- [ ] **Step 2: 검색 페이지**

`src/pages/search.astro`:
```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
const base = import.meta.env.BASE_URL;
---
<BaseLayout title="검색 | 우리 헌법 기본권">
  <h1>검색</h1>
  <link href={`${base}pagefind/pagefind-ui.css`} rel="stylesheet" />
  <div id="search"></div>
  <script src={`${base}pagefind/pagefind-ui.js`} is:inline></script>
  <script is:inline>
    window.addEventListener("DOMContentLoaded", () => {
      new PagefindUI({ element: "#search", showSubResults: true, translations: { placeholder: "권리·용어 검색" } });
    });
  </script>
</BaseLayout>
```

- [ ] **Step 3: 빌드 + 검색 확인**

Run:
```bash
npm run build
npm run preview
```
Expected: `dist/pagefind/` 생성. `/search`에서 "존엄" 검색 시 제10조가 결과로 나옴.

> 주의: 개발 서버(`astro dev`)에는 pagefind 인덱스가 없으므로 검색은 `build`+`preview`에서만 동작.

- [ ] **Step 4: 커밋**

```bash
git add package.json src/pages/search.astro
git commit -m "feat: static search with pagefind"
```

---

## Task 13: GitHub Pages 배포 설정

**Files:**
- Modify: `astro.config.mjs`
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 1: astro.config.mjs에 site/base 설정**

`astro.config.mjs` (사용자 저장소명에 맞게 `base` 조정; 예시는 `fundamentalrights`):
```js
import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://<github-username>.github.io",
  base: "/fundamentalrights",
});
```

> `base`를 설정하면 모든 내부 링크가 `import.meta.env.BASE_URL`을 쓰도록 이미 작성돼 있어 정상 동작한다. 커스텀 도메인/루트 배포 시 `base`를 `/`로 둔다.

- [ ] **Step 2: 배포 워크플로**

`.github/workflows/deploy.yml`:
```yaml
name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with: { path: dist }
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 3: 빌드 재확인 + 커밋**

Run: `npm run build`
Expected: base 경로 적용 후에도 빌드 성공.
```bash
git add astro.config.mjs .github/workflows/deploy.yml
git commit -m "chore: github pages deploy config"
```

---

## Task 14: 전체 콘텐츠 작성 (제2장 전체)

> 코드가 아니라 데이터 작성 단계. 각 파일은 Task 4의 프런트매터 스키마와
> `## 쉬운 말 / ## 보통 / ## 자세히` 본문 규칙을 따른다. 근거 자료(헌법 원문,
> 헌재 판례, 인권위 자료)를 바탕으로 초안을 작성하고, terms·quiz를 채운다.

작성할 권리 목록(파일명은 `조문-제목.md`):

- [ ] 제11조 평등권 — `11-평등권.md` (category: equality, order: 2)
- [ ] 제12조 신체의 자유 — `12-신체의-자유.md` (freedom, 3)
- [ ] 제13조 죄형법정주의·소급입법 금지 — `13-죄형법정주의.md` (freedom, 4)
- [ ] 제14조 거주·이전의 자유 — `14-거주이전의-자유.md` (freedom, 5)
- [ ] 제15조 직업선택의 자유 — `15-직업선택의-자유.md` (freedom, 6)
- [ ] 제16조 주거의 자유 — `16-주거의-자유.md` (freedom, 7)
- [ ] 제17조 사생활의 비밀과 자유 — `17-사생활의-자유.md` (freedom, 8)
- [ ] 제18조 통신의 자유 — `18-통신의-자유.md` (freedom, 9)
- [ ] 제19조 양심의 자유 — `19-양심의-자유.md` (freedom, 10)
- [ ] 제20조 종교의 자유 — `20-종교의-자유.md` (freedom, 11)
- [ ] 제21조 언론·출판·집회·결사의 자유 — `21-표현의-자유.md` (freedom, 12)
- [ ] 제22조 학문과 예술의 자유 — `22-학문예술의-자유.md` (freedom, 13)
- [ ] 제23조 재산권 — `23-재산권.md` (freedom, 14)
- [ ] 제24조 선거권 — `24-선거권.md` (participation, 15)
- [ ] 제25조 공무담임권 — `25-공무담임권.md` (participation, 16)
- [ ] 제26조 청원권 — `26-청원권.md` (claim, 17)
- [ ] 제27조 재판을 받을 권리 — `27-재판청구권.md` (claim, 18)
- [ ] 제28조 형사보상청구권 — `28-형사보상청구권.md` (claim, 19)
- [ ] 제29조 국가배상청구권 — `29-국가배상청구권.md` (claim, 20)
- [ ] 제30조 범죄피해자구조청구권 — `30-범죄피해자구조.md` (claim, 21)
- [ ] 제31조 교육을 받을 권리 — `31-교육권.md` (social, 22)
- [ ] 제32조 근로의 권리 — `32-근로의-권리.md` (social, 23)
- [ ] 제33조 노동3권 — `33-노동3권.md` (social, 24)
- [ ] 제34조 인간다운 생활을 할 권리 — `34-인간다운-생활.md` (social, 25)
- [ ] 제35조 환경권 — `35-환경권.md` (social, 26)
- [ ] 제36조 혼인·가족·보건 — `36-혼인가족보건.md` (social, 27)
- [ ] 제37조 기본권 제한의 일반원칙 — `37-기본권-제한.md` (comprehensive, 28)
- [ ] 제38조 납세의 의무 — `38-납세의무.md` (duty, 29)
- [ ] 제39조 국방의 의무 — `39-국방의무.md` (duty, 30)

각 항목 작성 후 주기적으로 검증:
```bash
npm run check && npm run build
```
Expected: 스키마 위반 없이 모든 권리 페이지 생성.

- [ ] **커밋(권리 묶음 단위로 자주):**
```bash
git add src/content/rights
git commit -m "content: add chapter 2 rights"
```

---

## Task 15: 최종 검증

- [ ] **Step 1: 전체 테스트**

Run: `npm test`
Expected: 모든 단위 테스트 PASS.

- [ ] **Step 2: 타입/콘텐츠 체크**

Run: `npm run check`
Expected: 0 errors.

- [ ] **Step 3: 빌드 + 미리보기 종합 점검**

Run:
```bash
npm run build
npm run preview
```
확인 항목:
- 홈에서 범주별 권리 카드가 모두 보임
- 권리 상세에서 난이도 3단 전환 동작, 새로고침 후 선택 유지(localStorage)
- 원문 펼침, 퀴즈 정답/오답·해설 표시
- 고대비·글자 크기 토글 동작, 새로고침 후 유지
- `/search`에서 검색 결과 표시
- 용어 사전·소개 페이지 표시, 면책 문구 노출
- 모바일 폭(반응형)·키보드 탐색·본문 바로가기 링크 동작
- 인쇄 미리보기에서 헤더/푸터 숨김, 본문 표시

- [ ] **Step 4: 최종 커밋**

```bash
git add -A
git commit -m "chore: final verification pass"
```

---

## 검증 기준 요약 (설계서 §10 대응)

- `npm run build` + `npm run check` 통과 (스키마 검증 포함)
- 난이도 토글·원문·퀴즈 실제 동작(미리보기 확인)
- Pagefind 검색 동작
- 반응형·고대비·글자확대·키보드 접근성 동작
- 출처·면책 문구 노출
