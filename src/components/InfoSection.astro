---
const { icon, title, text } = Astro.props;
---
<section style="margin:1.5rem 0">
  <h2 style="font-size:1.15rem;display:flex;gap:.4rem;align-items:center;margin:.2rem 0">{icon} {title}</h2>
  <p style="white-space:pre-line;margin:.4rem 0 0">{text}</p>
</section>
