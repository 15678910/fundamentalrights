---
const { name, alt = "", size = 56 } = Astro.props;
const base = import.meta.env.BASE_URL;
---
<img src={`${base}illustrations/${name}.svg`} alt={alt} width={size} height={size} loading="lazy" style="display:block;flex:none" />
